"""CLI tools for SR Tracker.

- `python -m tools.inject` — inject the hook DLL into the running game
- `python -m tools.REMOVED` — list exports of the compiled DLL
- `python -m tools.build` — produce a standalone exe via PyInstaller
"""
