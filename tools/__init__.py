"""CLI tools for SR Tracker.

- `python -m REMOVED` — inject the hook DLL into the game
- `python -m REMOVED.REMOVED` — list exports of the DLL
- `python -m tools.build` — produce a standalone exe via PyInstaller
"""
